import React from "react";
import { ThemeToggle } from "./ThemeToggle";

const Header = () => {
  return (
    <div>
      <span>Header</span> <ThemeToggle />
    </div>
  );
};

export default Header;
